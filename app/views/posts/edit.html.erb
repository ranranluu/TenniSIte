<%= form_with model: @post, local: true do |f| %>
  <div class="form-group">
    <%= f.label :content, class: "post_opinion" %>
    <%= f.text_area :content, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :image, class: "postimage_opinion" %>
    <%= f.attachment_field :image, class: "form-control" %>
  </div>
  <div class="form-group">
    <p>スペースを入力することで複数のタグを付けることができます。</p>
    <p>例：音楽 文芸 スポーツ</p>
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <%= f.submit 'Update Post', class: "btn btn-success col-xs-12" %>
  </div>
<% end %>